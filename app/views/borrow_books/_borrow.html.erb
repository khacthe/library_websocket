<div class="book_brow">
  <%= form_for(@borrow_book) do |f| %>
    <%= hidden_field_tag :book_id, @book.id %></div>
    <%= datetime_field :date_from %>
    <%= datetime_field :date_to %>
    <%= f.submit "Borrow", class: "btn btn-primary" %>
  <% end %>
</div>
